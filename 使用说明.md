# lcm-eval 复现脚本使用说明（reproduce.sh）

本目录提供一键复现论文《How Good are Learned Cost Models, Really? Insights from
Query Optimization Tasks》（SIGMOD 2025，DOI 10.1145/3725309，仓库
DataManagementLab/lcm-eval）的训练与评估实验的脚本。本文档是操作手册，说明每个
子命令、参数与完整流程；无需 AI 辅助即可照着复现。

## 1. 它能做什么

同一个脚本 reproduce.sh 支持：

| 能力 | 说明 |
| --- | --- |
| 本地 CPU 训练 | 一条命令跑全部或部分模型 |
| 本地评估（predict） | 按官方 exp_predict_all.py 分组评估 |
| T4 微调（retrain） | 官方 index_retraining / seq_retraining |
| 打包远端 GPU 训练 | 本地打包 → GPU 机器一键跑 → 结果导回 |
| 参数化选择 | 模型 / 目标库 / 随机种子 / 设备 / 并行度 |

核心原则：训练、评估、重训命令逐参数对应官方 exp_*.py，不新增官方 main.py 不
支持的参数；官方仓库 lcm-eval/ 与数据 data/runs/ 保持只读，派生文件只写
prepared/；所有路径基于脚本所在目录计算，整个目录可整体拷贝到任意机器。

## 2. 环境要求

- Linux（bash 4+），已安装 curl、git、tar、sha256sum
- 首次 setup 需要联网：GitHub、PyPI、download.pytorch.org、data.dgl.ai
- 本地 CPU 训练：建议内存 32GB 以上（flat 较快，神经模型 CPU 上偏慢）
- 远端 GPU：NVIDIA GPU + CUDA 11.8/12.1 驱动，无需 sudo（用 uv 装独立 Python）

## 3. 目录结构

    version pro/
    ├── reproduce.sh          一键复现主脚本（本手册对象）
    ├── prepare_stats.py      派生官方缺失统计文件的脚本（只读 OSF 数据）
    ├── README.md             概览文档
    ├── 使用说明.md            本文档
    ├── lcm-eval/             官方仓库（只读，setup 克隆）
    ├── data/runs/            官方 OSF 数据（只读；下载或 --link 软链复用）
    ├── prepared/             派生统计文件（setup 生成）
    ├── models/               训练产物（checkpoint）
    ├── evaluation/           评估产物（逐查询预测 CSV）
    ├── logs/                 训练 / 评估 / 重训日志
    ├── outgoing/             GPU 打包与结果归档（tar.gz + sha256）
    ├── gpu_results/          远端结果暂存
    ├── .venv/                本地 CPU Python 环境
    ├── .venv-gpu/            远端 GPU Python 环境（gpu-setup 创建）
    └── .tools/               uv 工具（setup 自动安装）

## 4. 快速开始（本地 CPU）

    # 1) 环境安装（克隆仓库 + 建 Python 环境 + 数据 + 派生统计）
    bash reproduce.sh setup

    # 如果你已有官方 runs 数据（如 v3 的 data/raw/runs），用只读软链复用：
    bash reproduce.sh setup --link /path/to/existing/runs

    # 2) 训练（默认全部模型 × 全部库 × 3 种子，共 63 个任务）
    bash reproduce.sh train

    # 3) 评估（默认全部，论文 E1-E4）
    bash reproduce.sh eval

所有子命令都支持 --dry-run，先打印官方命令、不真正执行，建议第一次先用它确认。

## 5. 子命令一览

    setup            环境安装（克隆官方仓库 + venv + 数据 + 派生统计）
    train            本地训练
    eval | predict   本地评估
    retrain          T4 微调（默认 zeroshot）
    gpu-bundle       打包自包含的远端 GPU 训练包（本地执行）
    gpu-setup        在远端 GPU 主机安装环境
    gpu-run          在远端 GPU 主机一键 训练(+评估)+打包
    gpu-pack         在远端 GPU 主机把结果打成 tar.gz + sha256
    import-results   校验并导入远端结果
    list-models      列出模型元数据
    help             显示帮助

## 6. 通用参数

以下参数适用于 train / eval / retrain / gpu-bundle / gpu-run：

| 参数 | 取值 | 默认 | 说明 |
| --- | --- | --- | --- |
| --models, -m | flat,zeroshot,dace,mscn,e2e,qppnet,query_former | 全部 7 个 | 逗号分隔，选择全部或部分模型 |
| --dbs, -d | imdb,baseball,tpc_h_pk | 全部 3 个 | 目标库 |
| --seeds, -s | 0,1,2 | 全部 3 个 | 随机种子 |
| --device | cpu 或 cuda:N | cpu | 训练设备 |
| --parallel, -P | 正整数 | 1 | 并行任务数 |
| --gpus | 0,1 | 无 | 多卡轮转（给任务分发 CUDA_VISIBLE_DEVICES） |
| --num-workers | 正整数 | 不传 | 追加官方可选的 --num_workers |
| --wandb-project | 字符串 | cost-eval | 覆盖 wandb 项目名 |
| --wandb-name | 字符串 | 模型/库/种子 | 覆盖 wandb 任务名 |
| --eval-set | core,paper,join_order_full,scan_costs_percentiles,physical_plan,physical_index_plan | core | 评估 workload 集合 |
| --test-workload | 文件路径 | 无 | 评估自定义 workload（可多次） |
| --with-eval | 开关 | 关 | 训练完成后自动评估 |
| --with-retrain | 开关 | 关 | 训练后自动做 T4 微调 |
| --force, -f | 开关 | 关 | 忽略已有 checkpoint 强制重跑 |
| --dry-run, -n | 开关 | 关 | 只打印官方命令不执行 |
| --out | 相对路径.tar.gz | 自动时间戳 | 指定打包产物文件名（相对本目录） |

模型类型（与官方 classes.py 一致）：

- workload-agnostic（用其余 19 库训练，目标库做 test）：flat、zeroshot、dace
- workload-driven（目标库单库训练，官方内部 80/10/10 划分）：mscn、e2e、qppnet、query_former

## 7. 本地训练 train

    # 全部 63 个任务
    bash reproduce.sh train

    # 部分模型 / 库 / 种子
    bash reproduce.sh train --models flat,zeroshot --dbs imdb --seeds 0,1

    # 本地有 CUDA 时用 GPU 训练
    bash reproduce.sh train --models e2e,mscn --device cuda:0 --parallel 2

    # 多卡轮转
    bash reproduce.sh train --models qppnet,e2e --gpus 0,1

    # 先看命令不执行
    bash reproduce.sh train --models flat --dbs imdb --seeds 0 --dry-run

脚本默认跳过已存在的 checkpoint（flat 为 models/flat/<db>_<seed>.txt，神经模型为
models/<model>/<db>/<model>_<seed>.pt），--force 可重训。日志在 logs/train/。

## 8. 本地评估 eval / predict

    # 全部（论文 E1-E4）
    bash reproduce.sh eval

    # 部分模型 / 库 / 种子
    bash reproduce.sh eval --models flat,mscn --dbs imdb --seeds 0

    # 只看某一类 workload
    bash reproduce.sh eval --eval-set physical_plan --dbs imdb
    bash reproduce.sh eval --eval-set core            # 论文全部（默认）

    # 自定义 workload 文件
    bash reproduce.sh eval --test-workload /path/to/workload.json

评估按官方 exp_predict_all.py 分组：把每个文件夹下的全部 *.json 一次传给
--test_workload_runs，输出写到 evaluation/<model>/<db>/<folder>/。没有对应
checkpoint 的模型会被跳过（需先 train）。

## 9. T4 微调 retrain

    bash reproduce.sh retrain                        # 默认 zeroshot × imdb × 3 seed
    bash reproduce.sh retrain --models zeroshot,dace --seeds 0

官方 exp_retrain_model.py as-is 只启用了 ZeroShotModelConfig（其余模型在文件里被
注释）。注意：官方 main.py 的 retrain 分支对 flat/e2e/mscn/query_former 会因
final_mlp_kwargs=None 在 main.py:370 抛 AttributeError（官方代码的已知限制）。
脚本对不支持模型仅提示、不阻断，命令仍按官方生成。运行前需先 train 对应模型的
imdb。

## 10. 打包远端 GPU 训练（本地执行）

### 10.1 本地打包

    # 打包全部模型（含数据与脚本），默认不评估
    bash reproduce.sh gpu-bundle

    # 部分模型 + 远端自动评估
    bash reproduce.sh gpu-bundle --models e2e,query_former --dbs imdb --seeds 0,1 --with-eval

    # 指定输出文件名
    bash reproduce.sh gpu-bundle --models zeroshot,dace --dbs imdb --seeds 0 --out outgoing/my_bundle.tar.gz

产物在 outgoing/lcm_gpu_bundle_<时间戳>.tar.gz 和同名 .sha256。包内包含官方源码、
reproduce.sh、prepare_stats.py、本库训练所需的数据（只拷需要的部分）以及
precomputed 的 prepared 统计，是自包含的。

### 10.2 在 GPU 机器上执行

上传两个文件（tar.gz + .sha256）到 GPU 服务器后：

    sha256sum -c lcm_gpu_bundle_*.tar.gz.sha256
    tar -xzf lcm_gpu_bundle_*.tar.gz
    cd lcm_gpu_bundle
    bash gpu-run.sh

gpu-run.sh 会自动完成：环境安装（gpu-setup：uv + Python 3.10 + torch cu121 +
dgl，首次需联网，无需 sudo）→ 训练 →（若打包时带 --with-eval 则评估）→（带
--with-retrain 则微调）→ 打包结果到 outgoing/lcm_gpu_results_<时间戳>.tar.gz。

### 10.3 导回本地

    bash reproduce.sh import-results /path/to/lcm_gpu_results_*.tar.gz

导入器先校验 sha256，再把 models/、evaluation/、logs/ 拷入本地对应目录
（--no-clobber，拒绝覆盖已有结果）。

## 11. 严格遵循官方命令的说明

脚本生成的命令与官方 exp 脚本逐参数一致，例如训练（对应 exp_train_model.py）：

    python main.py --mode train --wandb_project cost-eval --model_type <m>
      --device <d> --model_dir models/<m>/<db> --target_dir evaluation/<m>/<db>
      --workload_runs <19 个训练库 或 1 个目标库> --statistics_file <统计>
      --seed <s> --wandb_name <m>/<db>/<s>
      [--test_workload_runs ...] [--column_statistics ...] [--word_embeddings ...]
      [--hyperparameter_path ...]

评估（对应 exp_predict_all.py）：

    python main.py --mode predict --model_type <m>
      --test_workload_runs <某文件夹全部 json> --statistics_file <统计>
      --model_dir models/<m>/<db> --target_dir evaluation/<m>/<db>/<folder> --seed <s>
      [--column_statistics ...] [--word_embeddings ...] [--hyperparameter_path ...]

重训（对应 exp_retrain_model.py）：

    python main.py --mode retrain --wandb_project cost-eval-retrain
      --model_type <m> --device <d> --model_dir models/retrained/<m>/imdb
      --target_dir evaluation/retrained/<m>/imdb
      --workload_runs <index_retraining.json seq_retraining.json>
      --statistics_file <统计> --seed <s> --wandb_name <m>/imdb/<s>
      [--test_workload_runs ...] [--column_statistics ...] [--word_embeddings ...]
      [--hyperparameter_path ...]

注意：官方命令本就不带 --num_workers 与评估的 --device，脚本默认照抄官方，只有
显式传 --num-workers 才追加 --num_workers；不传 v3 私有补丁参数（--epochs、
--batch_size 等）。

## 12. 常见问题

- 脚本默认 WANDB_MODE=disabled（离线，不联网）。如需上报 wandb，运行前
  export WANDB_MODE=online 并自行登录。
- setup 网络慢被中断可重跑，已完成的步骤会跳过。
- 训练失败日志在 logs/<mode>/，文件名为 TRAIN__<模型>__<库>__seed<种子>.log、
  EVAL__...、RETRAIN__...。
- 断点续跑：已完成的 checkpoint 自动跳过；评估只需对应 checkpoint 存在。
- 磁盘占用：OSF 数据约 10GB；GPU bundle 每个约 100-500MB（取决于模型范围）。
- 先看命令再执行：所有子命令都支持 --dry-run。
- 首次 setup 若直连 PyPI 慢，可临时 export UV_INDEX_URL=<镜像> 加速（不影响脚本
  本身的可移植性）。

## 13. 本机隔离真跑验证（2026-08-25）

本脚本已在两个全新 `/tmp` 隔离目录中做过端到端验证。第二个目录仅预先放入
`reproduce.sh` 和 `prepare_stats.py`，其余内容均由 `setup` 自行完成：

    bash reproduce.sh setup --link /root/lcm模型/version3/data/raw/runs

验证环境与官方源码快照：

- 官方仓库由脚本重新 `git clone`，commit：
  `e024859de0d0b10f686e7da69759656d2a743057`，测试结束后 git 状态为干净。
- Python 3.10.21；torch 2.3.0+cpu；DGL 1.1.3；numpy 1.24.4；
  scipy 1.10.1；pandas 2.0.3；LightGBM 4.6.0。
- 本机没有 CUDA，`torch.cuda.is_available()` 为 False；因此本次只真跑 CPU，
  GPU 侧验证到可移动 bundle、逐文件哈希、结果打包和跨目录导入为止。

真实执行并通过的命令：

    bash reproduce.sh train --models flat --dbs imdb --seeds 0 --device cpu --with-eval
    bash reproduce.sh train --models mscn --dbs imdb --seeds 0 --device cpu --with-eval
    bash reproduce.sh train --models flat,mscn --dbs imdb --seeds 0 --device cpu

结果：

- flat：生成 2.83MB checkpoint，自动完成 imdb core 的 4 个评估任务；
  550 个评估 CSV，日志中无 Traceback/ERROR。
- mscn：在 epoch 58 触发官方早停，生成 29.05MB `.pt`；自动完成 4 个 core
  评估任务，共 278 个预测 CSV，日志中无 Traceback/ERROR。
- 重复执行能识别两个已有 checkpoint 并安全跳过，不会重复训练。
- 8 个官方空 workload 被明确跳过且各告警一次；所有非空 workload 的官方
  `main.py --mode predict` 参数保持不变。

GPU 归档与回传验证：

- mscn/imdb/seed0 + eval bundle 大小约 404MB；移动到另一目录后，外层
  `.sha256` 校验通过，包内 903 个文件逐项校验全部通过，软链接为 0。
- `gpu-pack --out <指定路径>` 能生成指定文件；结果包移动到第四个空目录后，
  `import-results` 校验并导入 1122 个文件，导入前后整树 SHA-256 完全一致。

隔离测试同时修复了四个不改变官方训练/评估参数的问题：空 workload 告警重复、
bundle 内部清单错误地包含自身、直接执行 `gpu-pack --out` 时参数未解析、外层
`.sha256` 记录本机绝对路径而不能随归档移动。
